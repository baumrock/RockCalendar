# Locales

## Adding a locale to your backend calendar

To add a locale to your backend calendar, you need to add the locale to the `config` object in the `backend.js` file and you need to load the locale js file via hook in `/site/templates/admin.php`:

